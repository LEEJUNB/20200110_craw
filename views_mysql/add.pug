html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='/topic') Server Side JS
        ul
            each topic in topics
                li
                    a(href='/topic/' + topic)= topic
        article
            if topic
                h2= topic.title
                = topic.description
                div= 'by '+topic.author
            else
                h2 welcome
                | this is serverside JS
        div
            a(href='/topic/add') add