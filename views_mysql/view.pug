html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='/topic') Server Side JS
        ul
            each topic in topics
                li
                    a(href='/topic/' + topic.id)= topic.title
            article
                if topic
                    h2= topic.title
                    = topic.description
                    div= 'by '+topic.author
                else
                    h2 Welcome
                    | This is server js.
            ul
                li
                    a(href='/topic/add') New Write Here
                if topic
                    li
                        a(href="/topic/'+topic.id+'/edit") edit
                    li
                        a(href="/topic/"+topic.id+'/delete') delete
                